write a function that takes in a list of integers and return True if it contains 007 in order

def spy_game(arr):
    code = [0, 0, 7]
    for num in arr:
        if num == code[0]:
            code.pop[0]
    return len(code) == 0 
